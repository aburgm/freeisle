#pragma once

#include "log/Logger.hh"

#include <map>
#include <string>

namespace freeisle::state::serialize {

struct AuxData {
  log::Logger &logger;
};

} // namespace freeisle::state::serialize
